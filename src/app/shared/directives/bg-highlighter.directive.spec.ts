import { BgHighlighterDirective } from './bg-highlighter.directive';

describe('BgHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new BgHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
